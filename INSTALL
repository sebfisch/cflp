# Installation Instructions

You can install the `cflp` package as follows.

 1. Unpack the sources and move into the source directory.

        > tar -xzf cflp-*.tar.gz
        > cd cflp-*

 2. configure, build, test, and install.

        > ./configure
        > make
        > make test
        > make install

